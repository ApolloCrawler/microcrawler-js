#! /usr/bin/env node

require("../node_modules/babel-register");

var process = require('process');

var Exporter = require('../lib/exporter').default;

var exporter = new Exporter();

exporter.main(process.argv);
