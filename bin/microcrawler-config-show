#! /usr/bin/env node

require("../node_modules/babel-register");

var process = require('process');

var Config = require('../lib/config').default;

var config = new Config();

config.show(process.argv);
