#! /usr/bin/env babel-node --

require("babel-register");

var process = require('process');

var Collector = require('../lib/collector').default;

var collector = new Collector();

collector.main(process.argv);
